<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="io.mochadwi">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.BIND_REMOTEVIEWS" />
    <uses-permission android:name="io.mochadwi.etalase.data.provider.READ_DATABASE" />
    <uses-permission android:name="io.mochadwi.etalase.data.provider.WRITE_DATABASE" />

    <permission
        android:name="io.mochadwi.etalase.data.provider.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="io.mochadwi.etalase.data.provider.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_tmdb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <provider
            android:name=".data.datasource.local.provider.EtalaseContentProvider"
            android:authorities="io.mochadwi.etalase.data.provider"
            android:exported="true"
            android:readPermission="io.mochadwi.etalase.data.provider.READ_DATABASE"
            android:writePermission="io.mochadwi.etalase.data.provider.WRITE_DATABASE" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="io.mochadwi.etalase.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>

        </provider>

        <receiver android:name="io.mochadwi.data.datasource.local.widget.CatalogueAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/catalogue_widgetinfo" />
        </receiver>

        <!-- 2. The service that will send our collection data to our widget. It needs the
                        permission android.permission.BIND_REMOTEVIEWS -->
        <service
            android:name="io.mochadwi.util.service.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <activity
            android:name="io.mochadwi.ui.SplashActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="io.mochadwi.ui.HomeActivity"
            android:configChanges="layoutDirection|orientation|screenSize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>

        </activity>

        <activity
            android:name="io.mochadwi.ui.NotifSettingActivity"
            android:configChanges="layoutDirection|orientation|screenSize"
            android:parentActivityName=".ui.HomeActivity" />

        <service
            android:name=".util.service.NotificationDailyService"
            android:exported="false" />

        <service
            android:name=".util.service.NotificationReleaseService"
            android:exported="false" />

    </application>

</manifest>